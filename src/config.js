/**
 * config.js — site-wide feature flags.
 *
 * Central place for simple on/off switches so behavior can be toggled without
 * editing JSX in multiple files.
 */

// Animated Squares background (reactbits). Set to false to hide it everywhere
// (home, articles, resume); set to true to re-enable.
export const SHOW_SQUARES = false;
